import mysql from 'mysql';
import validator from 'validator';
import connection from '../server';

// export const query = CREATE TABLE `serviceproyectdb`.`administradores` (
// `idAdministrador` INT(100) NOT NULL ,
// `nombreRazonSocialAdministrador` VARCHAR(100) NOT NULL ,
// `telefonoAdministrador` INT(10) NOT NULL ,
// `direccionAdministrador` VARCHAR(50) NOT NULL ,
// `emailAdministrador` VARCHAR(50) NOT NULL ,
// PRIMARY KEY (`idAdministrador`),
// UNIQUE `emailAdmin` (`emailAdministrador`),
// UNIQUE `telefonoAdmin` (`telefonoAdministrador`))
// ENGINE = InnoDB;

// // export default Administrador;
